%div
  = semantic_form_for [:admin, @template], remote: true do |f|
    = f.inputs do
      = f.input :type_id, as: :select, include_blank: false, collection: TemplateType.all
      = f.input :content, require: true
      = f.submit
